1、grouping.puml -> 分组
    目的是把一个流程下的不同业务，组合在一起；
    For Example:
        登录 -> 填写问卷 -> 自动领取积分
        一个流程的不同业务


2、swimlanes.puml -> 泳道
    目的是吧一个流程的不同的系统，组合在一起
    For Example:
        用户系统登录 -> 订单系统查询数据，返回用户系统


3、并行
    目的是把一个入口的东西进行拆分，走不同的业务逻辑
    For Example:
    用户申诉 -> 申诉不同业务 -> 业务处理
