原先的模式
    1、duck（抽象类）下有许多继承的类，如 BeiJingDuck, NanJingDuck, XiAnDuck
    2、每个鸭子的实现都是继承父类

    但是由于需求的变更，出现会飞的鸭子，会呱呱叫的鸭子，会吱吱叫的鸭子，这样就会使原先的架构不能继续支撑之前的业务
    一个鸭子既要会吱吱叫，还要会嘎嘎叫，这就不太好扩展

现在的模式
    将经常会变的行为，抽取出来搞成一个接口，不同的行为实现不同的接口，然后组合至不同的类中